# Local debug run, used only by run.sh (freeholdy reads docker-compose.yml alone).
services:
  backend:
    environment:
      DEBUG_AUTH_EMAIL: ${DEBUG_AUTH_EMAIL}
      SESSION_SECRET: ${SESSION_SECRET}
      PUBLIC_BASE_URL: http://localhost:${WEB_PORT:-8080}
