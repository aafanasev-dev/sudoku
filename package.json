{
  "name": "sudoku-five-levels",
  "version": "2.0.0",
  "private": true,
  "description": "A five-level Sudoku game with Fibonacci-gated unlocks, Google sign-in and server-side progress.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:backend": "cd backend && .venv/bin/pytest",
    "test:all": "npm test && npm run test:backend",
    "setup:backend": "cd backend && python3 -m venv .venv && .venv/bin/pip install -r requirements-dev.txt",
    "dev": "docker compose up --build",
    "down": "docker compose down"
  },
  "license": "MIT"
}
